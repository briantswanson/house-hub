# Automated routine definitions (P1)
# Each routine has: a schedule/trigger, a condition check, an action, and optional SMS notification
#
# Examples:
#   garage_open_at_night  — check every 15min after midnight, send SMS if open
#   appliance_done        — poll washer/dryer, send SMS when cycle finishes
